# 표심 · Pyosim — 개발 로드맵 (체크리스트)

> 기반 문서: [기획서.md](기획서.md)
> 작성 기준일: 2026-06-18
> 이 문서는 기획서의 「12. 개발 로드맵」을 실행 가능한 체크리스트로 분해한 것이다.
> 원칙: **위로 갈수록 쉽고, 아래로 갈수록 깊다.** 모든 단계는 "정치 모르는 사람이 이탈하지 않고 한 단계 더 들어오는가"를 최우선 기준으로 검증한다.

---

## 범례

- `[ ]` 미완료 / `[x]` 완료
- 🔴 **블로커** (이게 안 되면 다음 단계 불가)
- 🟡 **중립성·신뢰 관련** (기획서 2장 설계 원칙 직결, 타협 불가)
- 🤖 자동화 / 👤 사람(human-in-the-loop) 필요

---

## Phase 0 — 프로젝트 셋업 & 기반 공사

> 기획서에 명시되진 않았으나 Phase 1 착수 전 반드시 필요한 토대.

### 0-1. 저장소 · 개발 환경

- [x] 🔴 Git 저장소 초기화 (`main` 브랜치, autocrlf=false)
- [x] 모노레포 vs 멀티레포 결정 → **모노레포** (`frontend` / `backend` / `etl` / `infra` / `docs`)
- [x] `.gitignore`, `.editorconfig`, 커밋 컨벤션 정의 ([커밋_컨벤션.md](커밋_컨벤션.md))
- [x] README — 프로젝트 개요, 로컬 실행법
- [x] 라이선스 · 데이터 출처 고지 정책 문서화 ([데이터_출처_고지_정책.md](데이터_출처_고지_정책.md), 코드 라이선스는 배포 전 결정 TBD)

### 0-2. 기술 스택 부트스트랩

- [x] 프론트: Next.js + TypeScript 프로젝트 생성 (App Router, `frontend/`. `npm install` 은 사용자 환경에서)
- [x] 백엔드: FastAPI 프로젝트 생성 (`backend/`, requirements.txt + venv 안내)
- [~] DB: PostgreSQL 로컬 인스턴스 — `infra/docker-compose.yml` 작성 완료. **본 환경에 Docker 미설치**라 미구동 (사용자 환경에서 `docker compose up -d db` 또는 로컬 PG 사용)
- [x] ETL: 스케줄러/잡 러너 기본 골격 (`etl/jobs/run.py`, 잡 레지스트리 + `ping` 더미)
- [x] 환경변수 관리 (`.env.example` × 3, 실제 키는 `.env` 로 분리·gitignore)

### 0-3. 외부 API 사전 확보 🔴

- [x] 열린국회정보 (open.assembly.go.kr) API Key 발급 + **동작 검증 완료** (의원 3295건 조회)
- [ ] 선관위 (info.nec.go.kr) API Key 발급 (Phase 3 전 필요)
- [ ] 공공데이터포털 (data.go.kr) API Key 발급 (Phase 2 전 필요)
- [~] 각 API rate limit / 응답 스키마 / 호출 한도 문서화 ([외부_API.md](외부_API.md). 호출 **한도 수치는 포털 마이페이지 확인 TODO**)
- [x] API 응답 샘플 수집 → 데이터 모델 검증 (ALLNAMEMBER → [api_samples/](api_samples/))

### 0-4. 데이터 모델 초기 구축

- [x] 기획서 10장 스키마를 마이그레이션으로 구현 (`backend/app/models.py` + `migrations/versions/0001_initial_schema.py`)
  - [x] `Person`, `Party`, `District`
  - [x] `Bill`, `Vote`, `VoteRecord`
  - [x] `CriminalRecord`
  - [x] `Issue`, `Question`, `Answer`
  - [x] `Relationship` (🟡 `evidence_url` NOT NULL 제약)
- [x] 🟡 모든 데이터 테이블에 `source_url` / `last_verified` 필드 일관 적용
- [x] 시드 데이터 (정당 목록 + `color_hex`, 쟁점 7축) — `backend/app/seed.py`

> **Phase 0 잔여(사용자 환경 필요):** ① DB 기동(`docker compose up -d db` 또는 로컬 PG) → `alembic upgrade head` → `python -m app.seed` → 마일스톤 "로컬에서 수집 1건 → DB 저장 성공" 달성. ② 선관위·공공데이터포털 키 발급.

---

## Phase 1 — 테스트 MVP

> 목표: **이것만으로 바이럴 가능한 서비스 확보.**
> 테스트 → 결과(정당 일치율) → 공유 카드. 핵심 진입점 완성.

### 1-1. 쟁점 축 & 문항 설계 🟡 (가장 중요)

> 초안 작성 완료(4차 개편). **최근(2025년 이후) 표결 + 장단점 Ⓐ/Ⓑ** 8문항, 전부 status=초안:
> [문항_1차안.md](문항_1차안.md) · [seed_questions.py](../backend/app/seed_questions.py)
> 정치 용어 0 + 변별력 + 현재 의원 구성 표결. **공개 전 외부 교차검토 + 승인 전환 필수.**

- [x] 쟁점 축: 최근 정국 이슈 중심으로 재편(쟁점에 '교육' 추가). 안보·외교/환경·기후/젠더·가족은 최근 변별 표결 부재로 이번 세트 제외
- [x] 1차 세트 작성 — **8문항**(최근 2025년 이후 변별력 표결)
- [~] 🟡 문항 작성 규칙 준수 검증 — 규칙 반영, **외부 검수 대기**
  - [x] 법안명·정당명·정치용어 질문에 미포함 (생활 언어 + ▼출처)
  - [x] 양쪽 장점+단점 대칭 제시 (Ⓐ/Ⓑ 비교형, 입장형 아님)
  - [~] 어느 쪽도 도덕적으로 우월하게 들리지 않음 — 8·11번 톤 외부 체감 확인 필요
  - [x] "잘 모르겠어요" 항상 포함 (`AnswerChoice.모름`)
  - [x] 법안 출처·의안번호 ▼접기 연결 (`source_note`)
- [x] 🟡 문항↔법안 매핑 (`Question.bill_id`) — 11/11 변별력 있는 실제 의안 앵커
- [x] 정치 용어 0 / 이탈 방지 (1-3 진입원칙) — 장단점 비교형으로 무지식 응답 가능
- [ ] 🟡 외부 1~2인 교차검토로 편향 검증 (공개 전) 👤 — **잔여 차단 항목**
- [ ] 젠더·가족 축 처리 결정 (제외 유지 / 계류법안 앵커) 👤

### 1-2. 국회 API 연동 → 데이터 매핑 🔴

> 서비스 코드 확정·파이프라인 구현·SQLite 스모크 적재 검증 완료(300의원/30법안/285표결기록).
> 프로덕션 전수 적재는 DB 기동 후 `python -m jobs.run --job members|bills|vote_records` (사용자 환경).

- [x] 의안(Bill) 수집 파이프라인 (의안번호·법안명·상태·위원회) — `etl/jobs/ingest.py:run_bills`
- [x] 표결(Vote) + 표결기록(VoteRecord) 수집 (찬성/반대/기권/불참) — 집계 + 의원별
- [x] 인적사항(Person) 수집 (정당·지역) — `run_members`. (발의자 linkage 는 Phase 2-5)
- [x] 의안정보시스템 `billId` 직링크 매핑 — `Bill.assembly_bill_id` + `likms_url`
- [x] 🟡 수집 데이터에 1차 출처 링크 자동 부착 — `source_url`=likms `LINK_URL`, `last_verified`
- [x] 🤖 자체 DB 캐싱 (API rate limit 대응) — 멱등 upsert + 배치, `--limit` 상한
- [~] 매핑 정합성 검증 — 집계↔의원별 표결 소폭 불일치 확인·문서화([외부_API.md](외부_API.md)). 문항↔법안 매핑은 Phase 1-1 에서 연결

### 1-3. 테스트 UI (메인 진입점)

> 프로토타입 구현 완료. 프론트 `frontend/app/{page,test,result,share}`, 백엔드 공개 API
> `backend/app/api.py`(+`scoring.py`). 문항은 아직 초안이라 `?preview=1`로만 노출(1-1 외부검토 게이트).
> 백엔드는 Postgres 없이 SQLite로 end-to-end 검증(`backend/scripts/{verify_api,dev_sqlite}.py`).

- [x] 🟡 진입 화면 — 정치 용어 0, 법안목록 X, "내 정치성향 알아보기" (`app/page.tsx`)
- [x] 문항 진행 UI (8문항, 진행률 바 + 자동 진행 + 이전/다음) (`app/test/page.tsx`)
- [x] 선택지: Ⓐ/Ⓑ 장단점 비교 + "잘 모르겠어요"(`AnswerChoice.모름`)
- [x] 문항별 "원래 어떤 법안인지" 접기(▼) 출처 노출 (`source_note` + likms 링크)
- [x] 이탈 방지: 한 화면 한 문항, 중간 저장/세션 (`lib/session.ts` → `Answer.session_id`)
- [x] 모바일 우선 반응형 (max-width 640, 블록 버튼, 시스템 폰트)

### 1-4. 결과 화면

> 구현 완료. `app/result/{page,PartyChart}.tsx`, 채점 `backend/app/scoring.py`(로직 docstring 공개).

- [x] **개인 요약(라벨)** — 최다 일치 정당·%를 중립 서술(🟡 자극 라벨 없음)
- [x] **법안별 내 입장 vs 실제 표결** — 문항별 내 답 + 같은/다른 방향 표결 정당
- [x] **정당별 일치율** — 막대그래프 (Recharts, 정당 `color_hex`)
- [x] 🟡 **필수 고지문** — "표결 일치도일 뿐, 정치적 판단은 본인 몫" (`scoring.DISCLAIMER`)
- [x] 🟡 일치율 계산 로직 문서화·공개 — `scoring.py` docstring + API `method_note` 동봉

### 1-5. 공유 카드

> 구현 완료. `app/share/page.tsx`(OG 메타) + `app/api/og/route.tsx`(동적 이미지, edge 런타임).
> ⚠️ `@vercel/og`는 Windows `next start`에서 번들 폰트 경로 버그 → **edge 런타임**으로 회피(Linux 배포·로컬 모두 동작).

- [x] 🟡 "나" 중심 공유 카드 — 발견 프레이밍("나와 가장 많이 겹친 곳"), 자극 X
- [x] OG 이미지 생성 (동적, 한글 폰트 서브셋 번들 `scripts/fetch-og-font.mjs`)
- [x] 🟡 카드에도 출처·집계 기준 표기 ("실제 국회 표결 일치도 · 지지/반대 권유 아님")
- [x] 공유 링크 / SNS 메타태그 (`generateMetadata` openGraph + twitter, Web Share API/복사 폴백)

### 1-6. Phase 1 출시 체크  ← **진행 중**

- [x] 🟡 중립성 체크리스트(기획서 11장) 전 항목 점검 — [중립성_점검.md](중립성_점검.md) 작성. 코드 레벨 대부분 ✅, 잔여는 정책/운영(아래 ①②)
- [x] 기본 분석/로깅 (이탈 지점 측정 → "한 단계 더 들어오는가") — 익명 퍼널 이벤트(`Event` 모델 + `POST /api/events` + `frontend/lib/analytics.ts`). PII 미수집, sendBeacon 기반. `landing→test_start→question_view→answer→test_complete→result_view→share_click`(+source_open)
- [ ] 배포 (Vercel + Cloud Run 또는 Cloud Run 단독) 👤 — 사용자 환경/크레덴셜 필요
- [ ] 부하/rate limit 실측 👤 — 배포 후

> **Phase 1 잔여 차단(공개 전 필수):** ① 1-1 문항 외부 교차검토 → status 승인 전환 + 프론트 `fetchQuestions` 기본값 `preview=false` 전환(현재 preview로만 노출).
> ② Postgres 기동 + 실 ETL 적재(현재 채점은 `scoring.CURATED_PARTY_STANCES` 사실 테이블, 실 `VoteRecord` 폴백 로직 준비됨).

---

## Phase 2 — 참여 · 개인화 · 자동화

> 목표: 테스트가 "내 성향 확인"에서 끝나지 않고 **실제 정치 참여**로 이어지게.
> 서비스 목적의 완성형.

### 2-1. 입법예고 연결 (관심 → 참여)

- [ ] 입법예고(pal.assembly.go.kr) 상태 수집·매칭
- [ ] 🟡 의안번호·법안명 기준 매칭 (Bill ↔ likms ↔ pal)
- [ ] `Bill.pal_status` (진행중/종료) + `Bill.pal_url`
- [ ] 결과 화면 참여 연결 동선
  - [ ] 진행중 법안 → "지금 의견 낼 수 있어요" 배지 + 직링크
  - [ ] 종료 법안 → 표결 결과·찬반 데이터만
- [ ] 🟡 안전장치: 직접 제출 X, 공식 채널(국회)로만 연결 안내
- [ ] 🟡 매칭 오류 대응 (의안번호 기준, 상태 표기)

### 2-2. 자동 갱신 파이프라인 🤖

- [ ] 🤖 새 법안·표결 수집 (1일 1회)
- [ ] 🤖 입법예고 상태 동기화 (1일 1회)
- [ ] 🤖 표결된 주요 법안 선별 필터 (1일 1회)
- [ ] 🤖 오래된 문항 아카이브 (주 1회)
- [ ] 스케줄러: Cloud Scheduler / cron → ETL 잡 트리거
- [ ] 🟡 파이프라인 모니터링·실패 알림

### 2-3. 문항 초안 LLM 생성 + 어드민 검토 🟡  ← **구현 완료(코드)**

> 안전선: **데이터는 100% 자동, 문항은 반자동.**
> 구현: 백엔드 `app/llm_questions.py`(중립 프롬프트+생성) · `app/admin.py`(토큰인증 검토 API) ·
> 마이그레이션 `0005`(review_note). 프론트 `app/admin/page.tsx`(검토 큐 UI).
> SQLite end-to-end 검증: `backend/scripts/verify_admin.py`(인증/전이/공개노출/반려 통과).
> '반려'는 별도 enum 없이 `status=아카이브 + review_note('[반려] …')`로 표기(흐름 유지).

- [x] 🤖 LLM 문항 초안 생성 (법안 요약 → 중립 문항 초안 프롬프트) — `draft_question_for_bill`. **키(`ANTHROPIC_API_KEY`) 없으면 generate 비활성(503)**, 키는 사용자 환경
- [x] 🟡 중립 문항 프롬프트 설계 (양쪽 논리 대칭 강제) — 정치용어/법안명/정당명 0, Ⓐ/Ⓑ 장단점 대칭, 채점규약 고정. 정당 입장(찬/반)은 LLM이 만들지 않음(출처 원칙)
- [x] 👤 어드민 검토 대기열 UI (초안/수정/승인/반려) — 상태 탭 + 인라인 수정 + 전이 버튼
- [x] `Question.status` 흐름 (초안 → 검토중 → 승인 → 아카이브) — `admin._TRANSITIONS` 규칙 검증(잘못된 전이 409)
- [x] `Question.created_by` (auto/admin), `approved_by`, `approved_at` (+ `review_note` 추가)
- [x] 🟡 승인된 문항만 자동 공개 (반려/미승인은 비공개) — 승인 문항 **수정 시 검토중으로 환원**(재검토 강제)
- [~] 🟡 사용자 편향 신고 창구 (자동 생성 안전망) — **보류(제품 결정)**. 표결 데이터는 '사실'이라 신고 대상이 아니고, 신고가 의미 있는 대상은 LLM이 만든 '질문 표현'뿐. 현재 문항은 100% 사람 승인 게이트가 있어 공개 신고 창구는 과함 → 자동 생성 문항이 실제 공개될 때 '표현' 대상으로 재도입 검토

### 2-4. 개인화 — 내 의원 카드

- [ ] 지역 입력 → 선거구 매핑 (`District` ↔ `Person`)
- [ ] 의원 1명 카드: 사진·출석률·대표발의·주요표결·전과유무·출처
- [ ] 🟡 공유 가능한 완결물 (카드 단독으로 가치)
- [ ] 공공데이터포털 선거구 데이터 연동

### 2-5. 법안 상세

- [ ] 법안 상세 페이지 (대표·공동발의, 찬반 명단, 정당별 막대그래프)
- [ ] 처리 funnel 시각화 (발의 → 위원회 → 본회의 → 처리)
- [ ] 입법예고 링크 연결
- [ ] "지금 화제의 법안" 진입 (`Bill.is_featured`)

---

## Phase 3 — 아카이브 심화

> 코어 유저용. 큐레이션 어드민을 먼저 만들고 데이터를 채운다.

### 3-1. 정치인 전과기록 DB 🟡

- [ ] 선관위 공보 기반 수집 (`CriminalRecord`)
- [ ] 죄명·형량·확정여부·선고일·출처 (`source_url`)
- [ ] 🟡 동일 양식 표기 (특정인 부각 금지)
- [ ] 🟡 사실만 — 죄명/형량, 가치판단 배제
- [ ] 의원 카드 연결 (전과유무 표시)

### 3-2. 인물 타임라인

- [ ] 인물별 활동 타임라인 (발의·표결·이력)
- [ ] 🟡 모든 항목 1차 출처 동반

### 3-3. 사건 관계 시각화 (큐레이션) 🟡

- [ ] 👤 큐레이션 어드민 UI 먼저 구축
- [ ] 🟡 `Relationship.evidence_url` NOT NULL 강제 (엣지마다 근거 링크)
- [ ] 노드·엣지 그래프 렌더링 (Cytoscape.js)
- [ ] 직접 큐레이션 워크플로 (`curated_by`, `curated_at`)
- [ ] 규모↑ 시 PG 인접리스트 → Neo4j 전환 검토

---

## Phase 4 — 확장

> 시의성·깊이 고도화.

- [ ] 출석률 상세 분석
- [ ] 정치자금 (공공데이터포털)
- [ ] 공약 이행 추적
- [ ] 시의성 큐레이션 고도화 ("지금 화제의 법안" 자동 선별)
- [ ] 블로그 콘텐츠 엔진 (확산)
- [ ] 관계 그래프 Neo4j 본격 전환 (규모 기준 충족 시)

---

## 상시 운영 체크리스트 (Phase 무관, 지속)

### 중립성 ↔ 성장 균형 (기획서 11장) 🟡

> 1차 점검 결과: [중립성_점검.md](중립성_점검.md) (2026-06-19, Phase 1 MVP 기준). 코드 레벨 대부분 충족, 잔여는 외부검토·preview 전환.

- [ ] 문항: 양쪽 논리 대칭, "모름" 항상 제공, 법안명·정당명 질문에 미포함
- [ ] 결과: "일치도"로만 표현, "지지/반대해야 한다" 권유 금지
- [ ] 공유 카드·매칭 결과에도 출처·집계 기준 표기
- [ ] 의원 카드: 전과·출석률 동일 양식
- [ ] 정당 필터 기본값 "전체", 수집 로직 공개
- [ ] 자동 생성 문항은 반드시 사람 승인 후 공개

### 리스크 대응 모니터링 (기획서 13장)

- [ ] 문항 편향 논란 → 양쪽 대칭, 공개·피드백, "모름", 사람 승인
- [ ] 자동 생성 문항 오류 → 100% 사람 검토, 신고 창구
- [ ] "정당 추천" 오해 → "표결 일치도" 프레이밍, 필수 고지문
- [ ] 명예훼손/선거법 → 1차 출처 의무화, 사실만
- [ ] 확산이 자극으로 흐름 → 발견 프레이밍, 카드 출처 표기
- [ ] 입법예고 매칭 오류 → 의안번호 기준, 상태 표기, 직접 제출 안 함
- [ ] API rate limit → 자체 DB 캐싱, 배치 수집

---

## 다음 액션 (즉시 착수, 기획서 14장)

1. [x] 쟁점 축 확정 + 1차 문항 세트(법안 매핑 포함) 작성 → **Phase 1-1** (초안 완료, 외부 검토 대기)
2. [x] 테스트 → 결과 → 공유 카드 프로토타입 → **Phase 1-3 ~ 1-5** (백엔드 API + 프론트 세로 슬라이스, SQLite end-to-end 검증)
3. [~] Phase 1 출시 체크 → **Phase 1-6** — 중립성 점검·로깅 완료([중립성_점검.md](중립성_점검.md)). 배포·외부검토·실ETL 적재 잔여(👤)  ← **진행 중**
4. [x] LLM 문항 초안 생성 프롬프트 설계 + 어드민 검토 흐름 → **Phase 2-3** (구현 완료, `verify_admin.py` 통과. 실 LLM 생성은 키 필요)
5. [ ] 입법예고 상태 매핑 로직 → **Phase 2-1**  ← **다음(코드)**

---

## 마일스톤 요약

| Phase | 핵심 산출물 | 완료 기준 |
| ----- | ----------- | --------- |
| 0 | 저장소·스택·API Key·DB 스키마 | 로컬에서 수집 1건 → DB 저장 성공 |
| 1 | 테스트 → 결과 → 공유 카드 | 바이럴 가능한 MVP 배포 |
| 2 | 입법예고 연결 + 자동화 + 어드민 + 의원 카드 | 자동 갱신 + 사람 승인 흐름 가동 |
| 3 | 전과 DB · 타임라인 · 관계 그래프 | 코어 유저용 아카이브 공개 |
| 4 | 출석·정치자금·공약·시의성 | 확장 기능 안정화 |
