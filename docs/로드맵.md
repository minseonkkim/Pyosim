# 표심 · Pyosim — 개발 로드맵 (체크리스트)

> 기반 문서: [기획서.md](기획서.md) — **민심 vs 국회 / 시민 참여 플랫폼**
> 갱신 기준일: 2026-06-20 (실데이터 적재·세금 도구·프로필 타임라인 반영)
> 원칙: 통합 정체성 유지하되 **데이터 그물망을 단계적으로 확장**. 무관심층이 멈추지 않게.
>
> **현재 DB 실적재(2026-06-20 확인):** 의원 300명(전원 의원코드·정당·지역구·생년·성별·선수·직책·**사진**) ·
> 정당 8(색상 시드) · 법안 18,649(생활 카테고리 9,812 분류) · 표결집계 1,627 · 의원별 표결기록 460,096 ·
> 대표발의 연결 16,447건. **미적재:** 출석률(0) · 전과(0, OpenAPI 없음) · District 정규화(0).

## 범례
- `[ ]` 미완료 / `[x]` 완료 / `[~]` 부분
- 🟡 **중립성·신뢰 관련**(기획 1.3, 타협 불가) / 🤖 자동화 / 👤 사람 필요

---

## 0. 자산 재배치 (피벗 정리)

> 옛 제품(성향 테스트 단일)에서 만든 것 중 **대부분은 새 그물망의 부품으로 재사용**된다.
> 버리는 게 아니라 위치를 옮긴다.

### 유지 (그대로)
- [x] 디자인 시스템 Pyo Ink ([frontend/app/globals.css](../frontend/app/globals.css), [디자인_시스템.md](디자인_시스템.md))
- [x] 서비스명 `표심 · Pyosim`
- [x] 인프라: Next.js 셸(appbar/main 640) · FastAPI · alembic · 익명 세션 · 퍼널 로깅(Event)
- [x] ETL 인프라: `AssemblyClient`(🟡 UA 필수) · 잡 러너 · members/bills/vote_records 수집

### 재배치 (용도 변경)
- [~] 데이터 모델 `Person·Party·Bill·Vote·VoteRecord·District·CriminalRecord` → **세 축(사람·법안·지역) 그물망 뼈대** (Phase 1). 사람·법안·표결·발의자 실적재 완료. **위원회·청원·District 정규화는 확장 필요**
- [~] 성향 테스트(test/result/share/scoring/seed_questions) → **유입 장치(기획 5.1)**. 전체 제품이 아니라 진입점 하나로 격하·유지
- [~] `Bill.pal_status`/`pal_url` 필드(모델 0001에 존재) → **기능 B 4.4(입법예고 댓글)** 토대. 매칭/수집 로직은 Phase 2에서 신규 구축(옛 시도는 의안번호 정확매치라 폐기)

### 정리 완료 (2026-06-19)
- [x] 어드민 문항 검토 큐 삭제(`backend/app/admin.py`·`verify_admin.py`·`frontend/app/admin`·`lib/admin.ts` + main/config/.env 배선)
- [x] 옛 문항 문서 삭제(`문항_1차안.md`·`문항_생성_프롬프트.md`·`중립성_점검.md`)
- [x] 결과 화면 입법예고 per-문항 배지 제거(구조적 단절 — 참여는 기능 B로 이동)
- 참고: `Question.review_note` 컬럼/마이그레이션 0005는 무해한 미사용 필드로 남겨둠(제거 시 마이그레이션 churn)

---

## Phase 1 — 그물망의 뼈대

> 목표: 세 축(사람·법안·지역)을 연결 키로 묶고, **정치인 프로필·법안 페이지**라는 '한 곳에서 본다' 허브 완성. 유입은 기존 성향 테스트.

### 1-1. 데이터 축 & 연결 키 🔴
- [x] 의원 ID(`assembly_member_code`) · 법안 ID(`bill_no`/`assembly_bill_id`) — 모델·수집·**실적재 완료**(의원 300·법안 18,649·표결기록 460,096)
- [x] **발의자 linkage — 대표발의 연결**(`nzmimeepazxkubdpn`, `RST_MONA_CD`→`Person`). ETL `run_proposers`(`--job proposers`). 표결 안 된 계류 의안도 적재 → 프로필 '대표발의' 채움(실데이터 16,447건 연결). **공동발의는 Phase 2-5(법안 상세)**
- [ ] 위원회(Committee) 모델 + 의원↔위원회 매핑 (청원·법안 회부 연결 키) — 1-1 잔여. **API 확보됨(동일 키)**: 위원회 현황 `nxrvzonlafugpqjuh`(358) · 위원 명단 `nktulghcadyhmiqxi`(`MONA_CD` 매핑) · 의원 위원회경력 `nyzrglyvagmrypezq`(3,720) — 수집 잡만 추가하면 됨
- [ ] 지역구 코드 정규화 — `District` ↔ `Person` 주소 매핑(중앙선관위/행안부 행정구역코드) — 1-1 잔여
- [x] 🟡 모든 적재 데이터에 1차 출처 링크(likms `DETAIL_LINK`)·`last_verified` 일관 적용

### 1-2. 정치인 프로필 페이지 (사람 허브) — **구현(MVP 완료)**

> 백엔드 `app/persons.py`(`GET /api/persons`, `/{id}` 프로필) · 프론트 `app/persons`(목록·검색·정당필터) ·
> `app/person/[id]`(프로필 상세) · `app/persons/PersonBits.tsx`(아바타·정당점). 랜딩에 "국회의원 둘러보기" 진입.
> **실데이터 적재 완료(2026-06-20):** 현역 300명 전수(`--job members`) — `seed_demo.py` 데모는 더 이상 주 데이터 아님.
> 검증: `verify_api.py`(목록/검색/프로필/404/🟡 중립고지·출처필드 통과).

- [x] 현역 프로필: 정당·지역구·**사진** — **300명 전수 적재 완료**. 사진은 `ALLNAMEMBER`(역대 통합)의 `NAAS_PIC`을 `NAAS_CD==MONA_CD`로 매칭(`--job photos`, 300/300). **위원회만 1-1(Committee 모델)+상임위 API 후 잔여**
- [x] 발의/표결 법안 리스트 → 법안 페이지로 링크(그물망) — 대표발의 리스트 + 표결 요약. 법안 페이지(1-3) 전까진 likms 직링크
- [x] 대표발의 법안 **날짜 타임라인** — 발의일(왼쪽)·세로축·법안 카드(오른쪽), 최신순 정렬(`BillBrief.proposed_date`)
- [~] 전과(`CriminalRecord`)·출석률 — UI 양식·중립 고지·출처필드 완성. **단 실데이터 미적재**(전과 0건=선관위 소스 필요, 출석률 `attendance_rate` 0/300=표결기록서 산정 또는 별도 소스 필요)
- [x] 프로필 보강 — **생년(나이)·성별·선수(초선/재선)·직책 적재 완료**(`--job members`, person 마이그레이션 0008). 프로필 헤더에 표시. 🟡 국회 공식 데이터 기준 사실만
- [ ] 재산·병역(선관위) — Phase 2 데이터 확보 후. (선관위 키 필요 — 보유 키로 불가)
- [ ] 프로필 보강 2차(동일 키, 즉시 가능): SNS `negnlnyvatsjwocar`(300) · 의원이력 `nexgtxtmaamffofof`(615) · 위원회 경력 `nyzrglyvagmrypezq` · 국정감사/인사청문 연계
- [~] 공유 가능한 완결물 — 프로필 단독 페이지 완성. OG/공유 메타는 추후

### 1-3. 법안 페이지 (법안 허브) — **구현(MVP 완료)**

> 백엔드 `app/bills.py`(`GET /api/bills/{id}`) · 프론트 `app/bill/[id]`. 프로필 발의법안 →
> 내부 법안 허브로 연결(그물망 닫힘: 사람→법안→대표발의자·표결의원→다시 프로필).
> 검증: `verify_api.py`(법안 상세·대표발의·funnel·404) + 실데이터(찬250/반1·정당별·표결의원 285명).

- [x] 법안 상세: 대표발의 · 처리 funnel(발의→위원회→본회의→처리) — **공동발의·쉬운말 요약은 Phase 2-5**. **API 확보됨(동일 키)**: 공동발의=의안 제안자정보 `BILLINFOPPSR`; 단계별 **정확 일자 + 표결수치**=본회의 처리안건 `nwbpacrgavhjryiph`(22대 1,578)·의안 심사정보 `BILLJUDGE`(36,018) → funnel 을 실제 날짜 타임라인으로 승격 가능
- [x] 정당별 찬반 표결 막대 — 의원별 기록(VoteRecord) 있을 때 정당별 찬/반 분해 + 본회의 집계
- [x] 의안정보시스템(likms) 1차 출처 직링크 + 🟡 중립 고지
- [x] 표결한 의원 → 프로필 연결(그물망 닫힘) — 선택지 칩으로 의원별 프로필 링크
- [x] **제안이유·주요내용 본문** — likms 의안원문 HWP `PrvText` 추출(ETL `bill_content`). 🟡 공식 원문 그대로(요약·판정 X). 두 형식(`<제안이유 및 주요내용>` 결합형 / `<제안이유>`·`<주요내용>` 분리형) 처리
- [x] **AI 요약(좋은점/문제점)** — 원문은 그대로 두고 **좋은점/문제점만 LLM 양쪽 대칭 생성**. provider 분기(`SUMMARY_PROVIDER`): **로컬 Ollama**(기본·무료·무제한·데이터 로컬, `ollama pull qwen2.5:7b`) | Gemini(무료 티어 일일 20회 제한이라 부적합). 백엔드 `app/bill_summary.py`(urllib) + on-demand 캐싱(`bills.py`) + ETL `bill_summary` 배치. 🟡 원문과 분리 표시·AI 생성 고지·생성 모델(`summary_model`) 출처 기록·대칭 깨지면 미저장
- [ ] 관련 청원·입법예고(Phase 2에서 연결)

### 1-3b. 법안 피드 (큐레이션 홈) — **구현(MVP)**

> 흩어진 법안 대신 '논쟁이 있던' 정책 법안만 골라 보여주는 현관(무관심층 진입).
> 백엔드 `GET /api/bills`(정쟁·절차성 제외 → 반대표 많은 순 → 민주 vs 국힘 갈림 우선) ·
> 프론트 `app/bills`(찬반 미니바 + ⚡정당갈림 배지 + AI 좋은점/문제점 한 줄) · 랜딩 진입 추가.

- [x] 사실 기반 선별(추천 아님) — `discriminating_bills` 기준 재사용. 🟡 선별 기준 고지 동봉
- [ ] 피드 상위 법안 본문+요약 데우기(Ollama 배치) — 본문 likms 400 일부·요약 진행 필요
- [x] 카테고리/일상 관련성 태깅(세금·노동·주거 등)으로 "내 삶과 상관" 강화 — `Bill.category`(마이그레이션 0009) + ETL `--job categorize`(🟡 결정론적 키워드 분류, [etl/jobs/categorize.py](../etl/jobs/categorize.py) 규칙 공개). 12개 생활 카테고리(세금·주거·노동·복지·의료·교육·환경·교통·안전·디지털·먹거리·가족) 전수 분류(9,812/18,649). 피드 `?category=` 필터 + `GET /api/bills/categories` 칩(피드 universe 867건). 프론트 `/bills` 칩 필터·카드 #태그

### 1-4. 지역 입구 (우리 동네)
- [ ] 주소 입력 → 지역구 매핑 → 내 의원 프로필 진입
- [ ] (예산·청원 동의 현황은 Phase 2~3)

### 1-5. 유입 장치 — 성향 매칭 테스트 (재배치)
- [x] 테스트→결과(정당/의원 일치율)→공유 — 기존 구현 유지. 빠른 연타 시 자동진행 타이머 중첩으로 문항 건너뛰던 버그 수정(클릭 시점 목표 문항으로만 이동)
- [ ] 결과에서 **그물망으로 연결**: 일치한 의원 → 프로필, 앵커 법안 → 법안 페이지
- [ ] 진입 동선 재배치(메인=플랫폼, 테스트는 진입점 하나)

---

## Phase 2 — 민심 레이어

> 목표: 시민 발화(청원·입법예고 의견)를 추적하고, 민심 vs 국회 미스매치를 드러낸다.

- [ ] **기능 A 국민청원 민심 추적**: `Petition` 모델 + 국민동의청원 수집 → 5단계 처리 추적 카드("그 청원 지금 어디?"). 🟡 발안자 개인정보 최소화. **API 확보됨(동일 키)**: 청원 계류현황 `nvqbafvaajdiqhehi`(22대 286) + 처리현황 `ncryefyuaflxnqbqo`(`PROC_RESULT_CD` 포함). 상세·소개의원은 `PTTINFODETAIL`/`PTTINFOPPSR`(파라미터 필요)
- [ ] **기능 B-4.4 입법예고 댓글 vs 통과**: 법안별 시민 찬반 의견 집계 ↔ 가결 여부(우선 구현 추천). **목록 API 확보됨(동일 키)**: 진행중 `nknalejkafmvgzmpt`/종료 `nohgwtzsamojdozky`. ⚠️ '찬반 의견 카운트'까지 주는지 파라미터 채워 재확인 — 없으면 국민참여입법센터(opinion.lawmaking.go.kr) 무료 OC 키 병행
- [ ] **기능 B-4.2 관심도 vs 활동량**: 국민 관심 이슈 TOP ↔ 국회 발의/예산 분야. 🟡 관심도 측정 중립성 설계 관건
- [ ] **감시견 알림**: 청원·법안·의원 구독 → 진행 변화 시 푸시(리텐션 핵심)
- [ ] 🤖 자동 갱신 파이프라인(1일 1회) + 모니터링

---

## Phase 3 — 비교 대시보드 완성

- [~] **기능 B-4.6 세금 쓰임 vs 우선순위** — 진입 장치로 앞당겨 구현(`/tax`). 월급 → 소득세+부가세(추정) → 분야별 쓰임 도넛(열린재정 OPFI165 결산) + **'계획(본예산) vs 실제(결산)' 분야별 갭**(OPFI172). ETL `--job budget` → `frontend/lib/budget-data.json`. 🟡 전부 추정·판정 배제·출처 동봉. **남은 것: ③세부사업 드릴다운 → ④세부사업→법안·의원 그물망**. **추가 확보(동일 OFD 키)**: 부처별 결산 `OPFI166`(63) → '내 세금이 어느 부처로' 신축. ⚠️ '걷는 쪽'(조세지출·세목별 세입)은 data.go.kr 별도 키 필요
- [ ] 공약 vs 이행(매니페스토 연계) · 시급함 vs 처리속도 추가
- [ ] '우리 동네' 본격화(지역 예산 연결)
- [ ] 후원금-표결 상관 등 고리스크 분석은 검증 체계 후 신중 도입

### 후순위(법적·난이도 리스크)
> 후원금-표결 상관, 예산 상세, 지방의원·후보자 전체 망라.

---

## 상시 원칙 (기획 1.3 중립성) 🟡
- [ ] 사실만 표시·판정 배제("미이행" X → "9개월 멈춤" O)
- [ ] 모든 항목 공식 원본 링크 의무
- [ ] 모든 정당·인물 동일 기준·동일 양식
- [ ] 계산식(이행률 등) 알고리즘 공개
- [ ] 국회가 여론과 달랐을 때 '왜'도 병기(분노가 아닌 이해)

---

## 다음 액션 (즉시 착수)
1. [x] 기획서 교체 + 자산 재배치 정리 + 로드맵 재작성
2. [x] **Phase 1-2 정치인 프로필 페이지** — 의원·법안·표결 그물망 허브 구축 + **300명 전수 실적재 완료**
3. [x] 법안 페이지(1-3) — 상세·본문·AI 요약(좋은점/문제점) 완료
4. [x] 세금 도구(4.6) — 분야별 쓰임 + 계획 vs 실제(열린재정 연동)
5. [x] 법안 카테고리 태깅(1-3b) — 12개 생활 카테고리 분류 + 피드 칩 필터
6. [ ] **다음 후보(택1):** 위원회(1-1, **API 확보** `nxrvzonlafugpqjuh` 외) · 지역 입구(1-4, District 정규화 선행) · 피드 상위 법안 본문+요약 데우기(1-3b) · 세금 ③세부사업 드릴다운 · 미분류 법안(8,837) 규칙 보강
   - **신규 후보(이번에 API 확보됨, 동일 키):** 청원 추적(기능 A, `nvqbafvaajdiqhehi`) · 입법예고(기능 B-4.4, `nknalejkafmvgzmpt`) · 처리 타임라인 승격(1-3, `nwbpacrgavhjryiph`) · 공동발의(2-5, `BILLINFOPPSR`) · 부처별 세금(4.6, `OPFI166`)
   - 검증 도구: [etl/scripts/explore_catalog.py](../etl/scripts/explore_catalog.py) · 전체 카탈로그/필드: [외부_API.md](외부_API.md) "동일 키로 확장 가능한 서비스"
